{
  "installCommand": "npm install -g pnpm@10.2.1 && pnpm config set registry https://registry.npmjs.org/ && pnpm store prune && pnpm install --force --ignore-scripts",
  "buildCommand": "pnpm run build"
}
